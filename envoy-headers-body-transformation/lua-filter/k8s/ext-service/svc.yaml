apiVersion: v1
kind: Service
metadata:
  labels:
    choreo.component: ext-bal-service
  name: ext-bal-service
spec:
  ports:
  - name: proxy-http-port
    port: 9090
    protocol: TCP
    targetPort: http-port
  selector:
    choreo.component: ext-bal-service
